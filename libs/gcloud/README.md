# gcloud

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build gcloud` to build the library.

## Running unit tests

Run `nx test gcloud` to execute the unit tests via [Vitest](https://vitest.dev/).
