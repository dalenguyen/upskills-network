{
  "name": "data-access-firestore",
  "$schema": "../../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "libs/data-access/firestore/src",
  "projectType": "library",
  "tags": ["scope:data-access", "type:service"],
  "// targets": "to see all targets run: nx show project data-access-firestore --web",
  "targets": {
    "typecheck": {
      "executor": "nx:run-commands",
      "options": {
        "commands": [
          "tsc -p libs/data-access/firestore/tsconfig.lib.json --noEmit",
          "tsc -p libs/data-access/firestore/tsconfig.spec.json --noEmit"
        ],
        "parallel": false
      }
    }
  }
}
