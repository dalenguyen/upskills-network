{
  "name": "data-access-email",
  "$schema": "../../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "libs/data-access/email/src",
  "projectType": "library",
  "tags": [
    "scope:data-access",
    "type:service"
  ],
  "// targets": "to see all targets run: nx show project data-access-email --web",
  "targets": {
    "typecheck": {
      "executor": "nx:run-commands",
      "options": {
        "commands": [
          "tsc -p libs/data-access/email/tsconfig.lib.json --noEmit",
          "tsc -p libs/data-access/email/tsconfig.spec.json --noEmit"
        ],
        "parallel": false
      }
    }
  }
}
